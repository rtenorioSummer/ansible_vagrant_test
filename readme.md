# Vagrant - Ansible - OpenStack Tests

STATUS:

- Template can read vm_specific variable if is fiven as an ansible.extra_vars
- If so, it is just a variable defined in the VM which can be directly accesed